# 苏州德力 · 部署与配置全攻略

> 公众号：苏州德力 | 微信号：deli_photo
> GitHub Pages: https://feiteng68.github.io/suzhou-deli-products/
> 文档版本：2026年4月

---

## 一、GitHub Pages（已完成部署）

### 1.1 访问地址

产品展示页已部署到 GitHub Pages，直接在浏览器中访问：

**https://feiteng68.github.io/suzhou-deli-products/产品展示.html**

### 1.2 公众号菜单跳转配置

在公众号后台 → 「自定义菜单」中，添加菜单项：

1. 创建二级菜单，类型选择 **「跳转网页」**
2. 页面地址填入：`https://feiteng68.github.io/suzhou-deli-products/产品展示.html`
3. 注意：由于是外部链接，需要在公众号后台 「设置」→「公众号设置」→「功能设置」中，将 `feiteng68.github.io` 添加到 **JS接口安全域名**（如果使用 JS-SDK），同时公众号若需跳转外链，建议使用「阅读原文」或图文消息中的链接方式

> 备选方案：如果公众号不支持直接跳转外链，可将此链接放在图文消息的「阅读原文」中引导用户点击

### 1.3 更新产品

当需要更新产品时：

```bash
cd d:\微信公众号
# 修改 产品展示.html 或替换 image/ 中的图片
git add .
git commit -m "update: 更新产品信息"
git push
```

推送后 GitHub Pages 会自动更新（约1-2分钟生效）。

---

## 二、微信小程序

### 2.1 项目位置

```
d:\微信公众号\miniprogram\
```

### 2.2 页面结构

```
miniprogram/
├── app.js              # 全局配置（图片地址、联系方式）
├── app.json            # 路由和TabBar配置
├── app.wxss            # 全局样式
├── project.config.json # 项目配置（需替换AppID）
├── sitemap.json        # 站点地图
├── utils/
│   └── products.js     # 产品数据模块
├── images/             # TabBar图标（已生成）
│   ├── tab_home.png / tab_home_active.png
│   ├── tab_products.png / tab_products_active.png
│   └── tab_inquiry.png / tab_inquiry_active.png
└── pages/
    ├── index/          # 首页（品牌介绍、热门推荐、联系方式）
    ├── products/       # 产品列表（拼音首字母筛选、规格展示）
    ├── detail/         # 产品详情（大图预览、完整规格、简介）
    └── inquiry/        # 在线询价（表单填写）
```

### 2.3 导入步骤

1. **下载微信开发者工具**：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. **注册小程序账号**：https://mp.weixin.qq.com（选择小程序类型）
3. **打开开发者工具** → 选择「导入项目」
4. **项目目录**：`d:\微信公众号\miniprogram`
5. **AppID**：填入你的小程序 AppID

### 2.4 必须修改的配置

#### a) 替换 AppID

打开 `miniprogram/project.config.json`，找到：

```json
"appid": "YOUR_MINIPROGRAM_APPID"
```

替换为你的真实小程序 AppID。

#### b) 图片地址（已配置好）

小程序图片直接引用 GitHub Pages 上的图片，无需额外配置：

```javascript
// miniprogram/app.js 中已配置
imgBase: 'https://feiteng68.github.io/suzhou-deli-products/image/'
```

> 如后续需要迁移到自己的服务器或 COS，修改此地址即可

#### c) 联系方式

打开 `miniprogram/app.js`，修改 `globalData`：

```javascript
globalData: {
  imgBase: 'https://feiteng68.github.io/suzhou-deli-products/image/',
  serviceWeChat: 'deli_photo',  // 客服微信号
  phone: ''                      // 客服电话（如有）
}
```

### 2.5 配置下载域名

在小程序后台 → 「开发」→「开发管理」→「开发设置」→「服务器域名」中：

- **request合法域名**：添加 `https://feiteng68.github.io`
- **downloadFile合法域名**：添加 `https://feiteng68.github.io`

### 2.6 发布流程

1. **开发者工具预览**：点击「预览」按钮，扫码在手机上测试
2. **上传代码**：确认无误后点击「上传」，填写版本号和备注
3. **提交审核**：登录小程序后台 → 版本管理 → 提交审核
4. **发布上线**：审核通过后（通常1-3天），点击「发布」

---

## 三、公众号菜单配置方案

### 方案一：公众号菜单跳转 GitHub Pages（无需小程序）

```
一级菜单：产品展示
  └── 跳转网页 → https://feiteng68.github.io/suzhou-deli-products/产品展示.html

一级菜单：联系我们
  └── 跳转网页 → 微信客服页面 / 或直接回复微信号

一级菜单：更多
  └── 发送消息
```

### 方案二：公众号 + 小程序联动（推荐）

```
一级菜单：产品展示
  ├── 跳转小程序 → pages/products/products
  └── 跳转小程序 → pages/index/index

一级菜单：在线询价
  └── 跳转小程序 → pages/inquiry/inquiry

一级菜单：联系我们
  └── 发送消息 → 自动回复客服微信
```

**关联小程序步骤：**
1. 公众号后台 → 「小程序管理」→「关联小程序」
2. 输入小程序 AppID，发送邀请
3. 小程序后台确认关联
4. 关联后菜单中可使用「跳转小程序」功能

---

## 四、常见问题

**Q: GitHub Pages 页面打开很慢？**
A: 图片文件较大（每个约1-2MB），首次加载需要时间。后续浏览器会缓存。如需加速，可使用 CDN 或将图片上传到国内云存储。

**Q: 公众号菜单「跳转网页」提示不安全？**
A: GitHub Pages 使用 HTTPS，是安全的。如果仍然提示，可能是公众号未认证。个人订阅号可使用「阅读原文」方式外链。

**Q: 小程序图片加载不出来？**
A: 检查小程序后台是否已将 `feiteng68.github.io` 添加到合法域名。开发阶段可在开发者工具中勾选「不校验合法域名」。

**Q: 询价表单怎么接收用户提交？**
A: 当前版本为前端弹窗确认。如需后端接收，可选：① 微信云开发数据库 ② 腾讯云函数 ③ 第三方表单服务。

**Q: 如何更新产品信息？**
A: 修改 `miniprogram/utils/products.js` 中的产品数据，同时更新 `产品展示.html` 中的 `PRODUCTS` 数组和 `IMG_MAP`。

---

*文档生成时间：2026年4月*
